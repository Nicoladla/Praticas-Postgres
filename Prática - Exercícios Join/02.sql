SELECT testimonials.id, tw.name AS writer, tr.name AS recipient, testimonials.message FROM testimonials JOIN users AS tw ON testimonials."writerId" = tw.id JOIN users AS tr ON testimonials."recipientId" = tr.id;